#kafka config
spring:
  kafka:
    consumer:
      group-id: foo
      auto-offset-reset: latest
    bootstrap-servers: 172.31.31.252:9092
logging:
  level:
    org.boot.kafka.bootkafka: debug
  path: logs
